---
import PostPage from "../components/PostPage.astro";
import MainGridLayout from "../layouts/MainGridLayout.astro";
import { getSortedPosts } from "../utils/content-utils";
import { url } from "../utils/url-utils";

const chapters = (await getSortedPosts())
	.filter((post) => /^computer-review\/0[1-8]-/.test(post.slug))
	.sort((a, b) => a.slug.localeCompare(b.slug));
---

<MainGridLayout title="计算机复习 · 第 1～8 章" description="按章节阅读计算机基础、Windows、Office、计算机网络、多媒体与数据库知识总结。">
    <section class="card-base p-6 md:p-9 mb-4">
        <h1 class="text-3xl font-bold text-90 mb-4">计算机复习 · 第 1～8 章</h1>
        <p class="text-75 leading-relaxed mb-4">从计算机基础到 Windows、Office、网络、多媒体与数据库，按章节整理知识点、例题和易错点。选择下方章节开始复习。</p>
        <a class="link font-bold" href={url("/posts/computer-review/00-overview/")}>查看系列目录与阅读说明 →</a>
    </section>
    <PostPage page={{ data: chapters }} />
</MainGridLayout>
